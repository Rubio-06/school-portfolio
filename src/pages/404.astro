---
import BaseLayout from "@layouts/BaseLayout.astro";
import { Button } from "@ui/Button";
---

<BaseLayout>
  <section
    data-error-section
    class="relative flex min-h-[calc(100vh-73px)] items-center justify-center px-8"
  >
    <div class="relative z-10 mx-auto max-w-4xl text-center">
      <!-- Error Code with floating animation -->
      <div data-error-code class="mb-4">
        <h1
          class="bg-gradient-to-r from-blue-400 via-purple-400 to-pink-400 bg-clip-text text-9xl font-bold text-transparent md:text-[12rem] lg:text-[16rem]"
        >
          404
        </h1>
      </div>

      <!-- Glitch effect container -->
      <div data-error-title class="relative z-20 mb-6">
        <h2
          class="mb-2 bg-gradient-to-r from-white via-blue-100 to-purple-200 bg-clip-text text-3xl font-bold text-transparent md:text-5xl lg:text-6xl"
        >
          Page introuvable
        </h2>
      </div>

      <!-- Description -->
      <p data-error-description class="mb-8 text-lg text-gray-400 md:text-xl lg:text-2xl">
        Oups ! La page que vous recherchez semble s'être perdue dans le cyberespace...
      </p>

      <!-- Decorative elements -->
      <div data-error-decoration class="mb-12 flex items-center justify-center gap-4">
        <div class="h-1 w-16 rounded-full bg-gradient-to-r from-blue-500 to-purple-500"></div>
        <div
          class="flex h-12 w-12 items-center justify-center rounded-full border-2 border-white/20 bg-white/5 backdrop-blur-sm"
        >
          <svg
            class="h-6 w-6 text-purple-400"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
            ></path>
          </svg>
        </div>
        <div class="h-1 w-16 rounded-full bg-gradient-to-r from-purple-500 to-pink-500"></div>
      </div>

      <!-- CTA Buttons -->
      <div data-error-cta class="flex flex-col items-center justify-center gap-4 sm:flex-row">
        <Button client:load variant="primary" href="/"> Retour à l'accueil </Button>
      </div>

      <!-- Floating particles -->
      <div class="absolute inset-0 -z-10 overflow-hidden">
        <div
          data-particle
          class="absolute top-[20%] left-[10%] h-2 w-2 rounded-full bg-blue-400/30"
        >
        </div>
        <div
          data-particle
          class="absolute top-[30%] right-[15%] h-3 w-3 rounded-full bg-purple-400/30"
        >
        </div>
        <div
          data-particle
          class="absolute bottom-[25%] left-[20%] h-2 w-2 rounded-full bg-pink-400/30"
        >
        </div>
        <div
          data-particle
          class="absolute right-[25%] bottom-[35%] h-4 w-4 rounded-full bg-blue-400/20"
        >
        </div>
        <div
          data-particle
          class="absolute top-[40%] left-[70%] h-2 w-2 rounded-full bg-purple-400/30"
        >
        </div>
        <div
          data-particle
          class="absolute right-[60%] bottom-[20%] h-3 w-3 rounded-full bg-pink-400/20"
        >
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  import { init404Animations } from "@animations/error404";

  document.addEventListener("astro:page-load", () => {
    init404Animations();
  });
</script>
